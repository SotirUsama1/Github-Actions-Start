name: my first workflow | shell script
on: 
    push:
        branches:
            - main
jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v5.0.0

            - name: run script
              run: |
                chmod +x script.sh
                ./script.sh