name: my first workflow | shell script
on: 
    push:
        branches:
            - main
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: echo
              run: echo "CICD"